# Prochaines étapes — compatibilité

Ce fichier n'est plus maintenu comme liste de tâches active.

La prochaine action autorisée se trouve uniquement dans `docs/handoff/PROGRESS.json`, champ `next_action`, avec le contexte d'autorisation du lot courant.

Les règles permanentes restent dans `PROJECT-HANDBOOK.md` et les preuves détaillées dans `docs/handoff/`.

L'historique de cette ancienne liste reste disponible dans Git. Ne plus dupliquer ici l'avancement ou les autorisations.
