# Rotation des clés

Versionner les clés, prévoir une période de chevauchement, valider ancienne et nouvelle clé, retirer l’ancienne de façon contrôlée et auditer l’opération. Les webhooks peuvent accepter temporairement deux secrets.
