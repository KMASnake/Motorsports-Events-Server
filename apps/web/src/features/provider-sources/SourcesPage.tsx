import {useEffect,useState} from 'react';
import {EmptyState,PageHeader,Panel,StatusChip} from '../../design-system';
import * as api from './sourcesApi';
import type {Provider,ProviderChampionship} from './sourcesApi';

const defaults={name:'',adapter_key:'ocblacktop',base_url:'https://api.ocblacktop.com/v1',enabled:false,discovery_enabled:false,max_concurrency:1};
const quotaDefaults={short_window_seconds:null,short_limit:null,minute_limit:null,hourly_limit:null,daily_limit:null,monthly_limit:null,limits_source:'configured',reset_timezone:null,reset_at:null,minimum_interval_seconds:1,safety_margin_percent:5,current_reserve_mode:'percent',current_reserve_value:20,provider_timezone:'UTC'};
const mappingDocument={championshipIds:{},circuitIds:{},sessionTypes:{},statuses:{}};

export function SourcesPage(){const[providers,setProviders]=useState<Provider[]>([]),[selected,setSelected]=useState<Provider|null>(null),[links,setLinks]=useState<ProviderChampionship[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[editing,setEditing]=useState(false),[draft,setDraft]=useState(defaults),[secret,setSecret]=useState(''),[quota,setQuota]=useState<Record<string,unknown>>(quotaDefaults),[mapping,setMapping]=useState<{active:any;versions:any[]}|null>(null),[result,setResult]=useState<Record<string,unknown>|null>(null);
  async function load(){setLoading(true);setError('');try{const rows=await api.listProviders();setProviders(rows);const current=rows.find(row=>row.id===selected?.id)??rows[0]??null;setSelected(current);if(current){const related=await api.listChampionships(current.id);setLinks(related);setQuota({...quotaDefaults,...await api.quota(current.id)});if(related[0])setMapping(await api.mappings(related[0].id));else setMapping(null);}}catch(reason){setError((reason as Error).message)}finally{setLoading(false)}}
  useEffect(()=>{void load()},[]);
  async function choose(provider:Provider){setSelected(provider);setResult(null);try{const related=await api.listChampionships(provider.id);setLinks(related);setQuota({...quotaDefaults,...await api.quota(provider.id)});setMapping(related[0]?await api.mappings(related[0].id):null)}catch(reason){setError((reason as Error).message)}}
  async function saveProvider(){try{const body={name:draft.name,adapter_key:draft.adapter_key,config:{base_url:draft.base_url},enabled:draft.enabled,discovery_enabled:draft.discovery_enabled,max_concurrency:Number(draft.max_concurrency),current_year_reserve_percent:20,missing_cycles_threshold:3,log_retention_days:30};if(selected&&editing)await api.updateProvider(selected.id,body);else await api.createProvider(body);setEditing(false);setDraft(defaults);await load()}catch(reason){setError((reason as Error).message)}}
  function edit(){if(!selected)return;setDraft({name:selected.name,adapter_key:selected.adapter_key,base_url:String(selected.config.base_url??''),enabled:selected.enabled,discovery_enabled:selected.discovery_enabled,max_concurrency:selected.max_concurrency});setEditing(true)}
  async function credential(){if(!selected||!secret)return;try{await api.replaceCredential(selected.id,secret);setSecret('');await load()}catch(reason){setError((reason as Error).message)}}
  const link=links[0];
  return <><PageHeader title="SOURCES" subtitle="Administration et configuration des fournisseurs" action={<button className="danger" onClick={()=>{setSelected(null);setEditing(true);setDraft(defaults)}}>＋ Ajouter une source</button>}/>{error&&<div className="sources-error" role="alert">{error}</div>}{loading?<Panel title="SOURCES"><p className="sources-loading">Chargement…</p></Panel>:providers.length===0&&!editing?<Panel title="SOURCES"><EmptyState text="Aucun fournisseur configuré."/></Panel>:<div className="sources-layout"><Panel title="FOURNISSEURS"><div className="sources-list">{providers.map(provider=>{const credential=provider.secrets.some(item=>item.name==='api_key'&&item.configured),complete=credential&&provider.championship_count>0;return <button key={provider.id} className={selected?.id===provider.id?'active':''} onClick={()=>void choose(provider)}><strong>{provider.name}</strong><span>{provider.adapter_key}</span><StatusChip text={provider.enabled?'ACTIF':'PAUSE'} tone={provider.enabled?'green':'muted'}/><small>{credential?'Clé configurée':'Clé absente'} · {provider.championship_count} championnat(s) · {complete?'Configuration complète':'Configuration incomplète'}</small></button>})}</div></Panel><main>{editing?<ProviderForm value={draft} onChange={setDraft} onSave={()=>void saveProvider()} onCancel={()=>setEditing(false)}/>:selected&&<><PageHeader title={selected.name} subtitle={`Adaptateur ${selected.adapter_key}`} action={<button onClick={edit}>Modifier</button>}/><div className="sources-grid"><Panel title="CONFIGURATION"><Info rows={[['État',selected.state],['Activé',selected.enabled?'Oui':'Non'],['Discovery',selected.discovery_enabled?'Oui':'Non'],['Concurrence',String(selected.max_concurrency)],['URL de base',String(selected.config.base_url??'—')]]}/></Panel><Panel title="CREDENTIAL"><div className="sources-form"><b>{selected.secrets.some(item=>item.name==='api_key'&&item.configured)?'Clé configurée':'Clé absente'}</b><input aria-label="Nouvelle clé API" type="password" autoComplete="new-password" value={secret} onChange={event=>setSecret(event.target.value)} placeholder="Nouvelle clé (jamais relue)"/><button disabled={!secret} onClick={()=>void credential()}>Remplacer la clé</button></div></Panel></div><Panel title="CHAMPIONNATS ASSOCIÉS">{links.length?<table className="data-table"><thead><tr><th>CHAMPIONNAT</th><th>EXTERNAL ID</th><th>ÉTAT</th><th>PRIMAIRE</th><th>SOURCE</th></tr></thead><tbody>{links.map(item=><tr key={item.id}><td>{item.championship_name}</td><td>{item.external_championship_id}</td><td>{item.sync_state}</td><td>{item.is_primary?'Oui':'Non'}</td><td>{String(item.source_config?.strategy??'—')}</td></tr>)}</tbody></table>:<EmptyState text="Aucun championnat/provider associé."/>}<ChampionshipEditor provider={selected} championship={link} onSaved={load} onError={setError}/></Panel>{link&&<><Panel title="QUOTA / CADENCE"><JsonForm value={quota} onSave={value=>api.saveQuota(selected.id,value).then(load).catch(reason=>setError(reason.message))}/></Panel><Panel title="NORMALIZATION MAPPING"><p>Actif : <b>{mapping?.active?.versionLabel??'Aucun'}</b> · {mapping?.versions.length??0} version(s) conservée(s)</p><MappingForm championship={link} onSave={value=>api.createMapping(link.id,value).then(load).catch(reason=>setError(reason.message))}/></Panel><Panel title="VALIDATION ZERO-CREDIT"><button onClick={()=>api.preflight(link.id).then(setResult).catch(reason=>setError(reason.message))}>Vérifier la configuration</button>{result&&<pre className="preflight-result">{result.status==='preflight_ok'?'Configuration valide':'Configuration invalide'}{`\nRequêtes fournisseur : ${String(result.PROVIDER_CALLS??0)}\n${String(result.reason??'Toutes les préconditions sont satisfaites.')}`}</pre>}</Panel></>}</>}</main></div>}</>}

function ProviderForm({value,onChange,onSave,onCancel}:{value:typeof defaults;onChange:(value:typeof defaults)=>void;onSave:()=>void;onCancel:()=>void}){return <Panel title="CONFIGURATION FOURNISSEUR"><div className="sources-form"><label>Nom<input value={value.name} onChange={e=>onChange({...value,name:e.target.value})}/></label><label>Adaptateur<select value={value.adapter_key} onChange={e=>onChange({...value,adapter_key:e.target.value})}><option value="ocblacktop">OCBlackTop</option><option value="thesportsdb">TheSportsDB</option></select></label><label>URL de base<input type="url" value={value.base_url} onChange={e=>onChange({...value,base_url:e.target.value})}/></label><label>Concurrence<input type="number" min="1" max="100" value={value.max_concurrency} onChange={e=>onChange({...value,max_concurrency:Number(e.target.value)})}/></label><label><input type="checkbox" checked={value.enabled} onChange={e=>onChange({...value,enabled:e.target.checked})}/> Activé</label><label><input type="checkbox" checked={value.discovery_enabled} onChange={e=>onChange({...value,discovery_enabled:e.target.checked})}/> Discovery activée</label><div><button className="danger" onClick={onSave}>Enregistrer</button><button onClick={onCancel}>Annuler</button></div></div></Panel>}
function Info({rows}:{rows:string[][]}){return <dl className="sources-info">{rows.map(([label,value])=><div key={label}><dt>{label}</dt><dd>{value}</dd></div>)}</dl>}
function JsonForm({value,onSave}:{value:Record<string,unknown>;onSave:(value:Record<string,unknown>)=>void}){const[text,setText]=useState(JSON.stringify(value,null,2));return <div className="sources-form"><textarea aria-label="Configuration quota" value={text} onChange={e=>setText(e.target.value)}/><button onClick={()=>onSave(JSON.parse(text))}>Enregistrer quota</button></div>}
function MappingForm({championship,onSave}:{championship:ProviderChampionship;onSave:(value:unknown)=>void}){const[label,setLabel]=useState(''),[rules,setRules]=useState('v1'),[text,setText]=useState(JSON.stringify({...mappingDocument,championshipIds:{[championship.external_championship_id]:championship.championship_id}},null,2));return <div className="sources-form"><label>Version<input value={label} onChange={e=>setLabel(e.target.value)}/></label><label>Règles<input value={rules} onChange={e=>setRules(e.target.value)}/></label><textarea aria-label="Mapping structuré" value={text} onChange={e=>setText(e.target.value)}/><button onClick={()=>onSave({version_label:label,rules_version:rules,mapping_document:JSON.parse(text)})}>Créer et activer une nouvelle version</button></div>}
function ChampionshipEditor({provider,championship,onSaved,onError}:{provider:Provider;championship?:ProviderChampionship;onSaved:()=>Promise<void>;onError:(value:string)=>void}){const[canonical,setCanonical]=useState(championship?.championship_id??''),[external,setExternal]=useState(championship?.external_championship_id??''),[syncState,setSyncState]=useState<'inactive'|'active'|'paused'>(championship?.sync_state??'inactive'),[primary,setPrimary]=useState(championship?.is_primary??false),[strategy,setStrategy]=useState(String(championship?.source_config?.strategy??'series-events-v1')),[endpoint,setEndpoint]=useState(String(championship?.source_config?.endpoint_template??'/{series}/events'));
  async function save(){try{if(championship){await api.updateChampionship(championship.id,{external_championship_id:external,is_primary:primary});await api.updateSourceConfig(championship.id,{external_id:external,strategy,endpoint_template:endpoint});if(syncState!==championship.sync_state)await api.updateSyncState(championship.id,syncState,championship.sync_state);}else await api.createChampionship(provider.id,{championship_id:canonical,source_config:{external_id:external,strategy,endpoint_template:endpoint}});await onSaved()}catch(reason){onError((reason as Error).message)}}
  return <div className="sources-form sources-championship"><label>Championnat canonique<input value={canonical} disabled={Boolean(championship)} onChange={e=>setCanonical(e.target.value)} placeholder="f1"/></label><label>External ID<input value={external} onChange={e=>setExternal(e.target.value)}/></label><label>Stratégie<input value={strategy} onChange={e=>setStrategy(e.target.value)}/></label><label>Modèle endpoint<input value={endpoint} onChange={e=>setEndpoint(e.target.value)}/></label>{championship&&<><label>État<select value={syncState} onChange={e=>setSyncState(e.target.value as typeof syncState)}><option value="inactive">Inactif</option><option value="active">Actif</option><option value="paused">Pause</option></select></label><label><input type="checkbox" checked={primary} onChange={e=>setPrimary(e.target.checked)}/> Source primaire</label></>}<button onClick={()=>void save()}>{championship?'Enregistrer association et source':'Associer le championnat'}</button></div>}
