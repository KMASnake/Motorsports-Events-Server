global:
  scrape_interval: 30s

rule_files:
  - /etc/prometheus/alert-rules.yml

scrape_configs:
  - job_name: motorsports-events-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
