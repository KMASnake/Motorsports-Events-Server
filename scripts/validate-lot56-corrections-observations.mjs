import assert from 'node:assert/strict';
import {randomUUID} from 'node:crypto';
import {pool} from '../apps/api/dist/lib/db.js';
import {AcquisitionTransactionService} from '../apps/api/dist/providers/acquisitionTransactionService.js';
import {SourceProtectionService} from '../apps/api/dist/providers/sourceProtectionService.js';
import {PersistentSchedulerService} from '../apps/api/dist/providers/schedulerService.js';
import Fastify from 'fastify';
import {AcquisitionAdminService} from '../apps/api/dist/providers/acquisitionAdminService.js';
import {providerAcquisitionAdminRoutes} from '../apps/api/dist/routes/providerAcquisitionAdmin.js';
import {registerAdminAuth,signAdminToken} from '../apps/api/dist/lib/adminAuth.js';

const phase=process.argv[2];assert.ok(phase==='phase-a'||phase==='phase-b');
const provider='56f00000-0000-4000-8000-000000000001',link='56f00000-0000-4000-8000-000000000002',stream='56f00000-0000-4000-8000-000000000003';
const clock={now:()=>new Date('2026-08-21T12:00:00Z')},scheduler=new PersistentSchedulerService(clock),acquisition=new AcquisitionTransactionService(scheduler,clock),protection=new SourceProtectionService();
const gate={beforeRequest:async()=>({allowed:true}),afterResponse:async()=>{},afterError:async()=>{}};
const fetchInput={providerInstanceId:provider,providerConfig:{},credentials:{},requestGate:gate,providerChampionshipId:link,championshipId:'lot56-f',sourceConfig:{},phase:'current',season:1969,cursor:{},signal:new AbortController().signal};
const item=name=>({entityKind:'event',externalId:'protected-event',identityIsSynthetic:false,parentExternalId:null,parentEntityKind:null,season:1969,sourceData:{name,status:'scheduled',starts_at:'1969-07-20T20:17:00Z'}});
const result=(items,page,complete=true)=>({status:'complete',items,itemAnomalies:[],nextCursor:{page},requestCount:1,complete,completionReason:complete?'end_of_collection':null});
const adapter=value=>({fetchWorkUnit:async input=>{await input.requestGate.beforeRequest();return value;}});
const lease=async worker=>{const generation=Number((await pool.query('select lease_generation from sync_streams where id=$1',[stream])).rows[0].lease_generation)+1,runId=randomUUID();await pool.query(`update sync_streams set state='running',lease_owner=$2,lease_acquired_at=$3,lease_expires_at=$4,lease_generation=$5 where id=$1`,[stream,worker,clock.now(),new Date(clock.now().getTime()+600000),generation]);await pool.query(`insert into sync_runs(id,stream_id,worker_id,lease_generation,work_class,cursor_before,status,request_id) values($1,$2,$3,$4,'current','{}','running',$5)`,[runId,stream,worker,generation,randomUUID()]);return {streamId:stream,runId,workerId:worker,generation};};
const execute=async(value,label,beforeCommit)=>acquisition.executeUnit({providerInstanceId:provider,providerChampionshipId:link,season:1969,workClass:'current_global',safeUnitKey:label,lease:await lease(label),adapter:adapter(value),fetchInput,beforeCommit});
const scalar=async(sql,params=[])=>(await pool.query(sql,params)).rows[0];

try{
  if(phase==='phase-a'){
    await pool.query(`insert into championships(id,slug,name,season,active,sync_enabled) values('lot56-f','lot56-f','Lot 5.6-F',1969,true,false)`);
    await pool.query(`insert into provider_instances(id,adapter_key,name,enabled,state) values($1,'lot56-f-fixture','Lot 5.6-F',true,'active')`,[provider]);
    await pool.query(`insert into provider_championships(id,provider_instance_id,championship_id,external_championship_id,discovery_state,sync_state,is_primary) values($1,$2,'lot56-f','lot56-f','configured','active',true)`,[link,provider]);
    await pool.query(`insert into sync_streams(id,provider_championship_id,phase,state,cursor_version,cursor) values($1,$2,'current','ready',1,'{}')`,[stream,link]);
    await execute(result([item('Provider A')],1),'seed-a');
    const entity=await scalar(`select id,source_data,provider_started_at from provider_source_entities where external_id='protected-event'`);assert.equal(entity.source_data.name,'Provider A');assert.equal(new Date(entity.provider_started_at).getUTCFullYear(),1969);
    const correction=await protection.upsertCorrection({sourceEntityId:entity.id,fieldPath:'name',overrideValue:'Local name',origin:'administrator',actorId:'maintainer',reason:'Verified local title'});
    await protection.upsertObservation({sourceEntityId:entity.id,observationKey:'timing:review',observationKind:'review_note',details:{note:'Local observation'},origin:'administrator',actorId:'maintainer',reason:'Track timing'});
    await execute(result([item('Provider A')],2),'replay-a');
    assert.equal(Number((await scalar('select count(*)::int count from provider_source_corrections where source_entity_id=$1',[entity.id])).count),1);assert.equal(Number((await scalar('select count(*)::int count from provider_source_local_observations where source_entity_id=$1',[entity.id])).count),1);
    await execute(result([item('Provider B')],3),'provider-b');
    assert.equal((await scalar('select source_data from provider_source_entities where id=$1',[entity.id])).source_data.name,'Provider B');assert.equal((await scalar('select override_value from provider_source_corrections where id=$1',[correction.id])).override_value,'Local name');assert.equal((await scalar(`select manual_override_active from provider_source_changes where source_entity_id=$1 order by id desc limit 1`,[entity.id])).manual_override_active,true);
    await protection.deactivateCorrection(correction.id);assert.equal((await scalar('select status from provider_source_corrections where id=$1',[correction.id])).status,'inactive');
    await protection.upsertCorrection({sourceEntityId:entity.id,fieldPath:'name',overrideValue:'Local current',origin:'administrator',actorId:'maintainer',reason:'Replacement'});
    const partial=await execute(result([],4,false),'partial-empty');assert.equal(Number((await scalar(`select count(*)::int count from provider_source_observations where traversal_id=$1 and observation_kind='not_observed'`,[partial.traversalId])).count),0);
    const complete=await execute(result([],5,true),'complete-empty');assert.equal(Number((await scalar(`select count(*)::int count from provider_source_observations where traversal_id=$1 and source_entity_id=$2 and observation_kind='not_observed'`,[complete.traversalId,entity.id])).count),1);
    await Promise.all([execute(result([item('Provider C')],6),'concurrent-c'),protection.upsertCorrection({sourceEntityId:entity.id,fieldPath:'name',overrideValue:'Concurrent local',origin:'administrator',actorId:'concurrent-user',reason:'Concurrent correction'}),protection.upsertObservation({sourceEntityId:entity.id,observationKey:'timing:review',observationKind:'review_note',details:{note:'Concurrent observation'},origin:'administrator',actorId:'concurrent-user',reason:'Concurrent review'})]);
    assert.equal((await scalar('select source_data from provider_source_entities where id=$1',[entity.id])).source_data.name,'Provider C');
    const rollback=await pool.connect();try{await rollback.query('begin');await protection.upsertCorrectionClient(rollback,{sourceEntityId:entity.id,fieldPath:'status',overrideValue:'local-status',origin:'administrator',actorId:'rollback-user',reason:'rollback'});await rollback.query('rollback');}finally{rollback.release();}assert.equal(Number((await scalar(`select count(*)::int count from provider_source_corrections where source_entity_id=$1 and field_path='status'`,[entity.id])).count),0);
    const protectedBefore=await scalar(`select (select count(*)::int from provider_source_corrections where source_entity_id=$1) corrections,(select count(*)::int from provider_source_local_observations where source_entity_id=$1) observations`,[entity.id]);
    const stale=await lease('stale-worker');await pool.query('update sync_streams set lease_generation=lease_generation+1 where id=$1',[stream]);await assert.rejects(()=>acquisition.executeUnit({providerInstanceId:provider,providerChampionshipId:link,season:1969,workClass:'current_global',safeUnitKey:'stale-protected',lease:stale,adapter:adapter(result([item('Stale')],7)),fetchInput}),/stale_worker/);
    await pool.query(`update sync_runs set status='interrupted',finished_at=now() where id=$1`,[stale.runId]);await pool.query(`update sync_streams set state='ready',lease_owner=null,lease_acquired_at=null,lease_expires_at=null where id=$1`,[stream]);
    await assert.rejects(()=>execute(result([item('Crash')],7),'crash-protected',async()=>{throw new Error('simulated_crash');}),/simulated_crash/);await pool.query(`update sync_runs set status='interrupted',finished_at=now() where stream_id=$1 and status='running'`,[stream]);await pool.query(`update sync_streams set state='ready',lease_owner=null,lease_acquired_at=null,lease_expires_at=null where id=$1`,[stream]);
    const protectedAfter=await scalar(`select (select count(*)::int from provider_source_corrections where source_entity_id=$1) corrections,(select count(*)::int from provider_source_local_observations where source_entity_id=$1) observations`,[entity.id]);assert.deepEqual(protectedAfter,protectedBefore);
    assert.equal((await scalar('select cursor from sync_streams where id=$1',[stream])).cursor.page,6);
    console.log('Phase A : source/correction/observation, replay, absence, concurrence, rollback, stale et crash : OK');
  }else{
    const entity=await scalar(`select id,source_data from provider_source_entities where external_id='protected-event'`);assert.equal(entity.source_data.name,'Provider C');
    const correction=await scalar(`select override_value,origin,actor_id,reason,status,revision from provider_source_corrections where source_entity_id=$1 and status='active'`,[entity.id]);assert.equal(correction.override_value,'Concurrent local');assert.equal(correction.actor_id,'concurrent-user');
    const observation=await scalar(`select details,origin,actor_id,reason,revision from provider_source_local_observations where source_entity_id=$1`,[entity.id]);assert.equal(observation.details.note,'Concurrent observation');assert.equal(observation.actor_id,'concurrent-user');
    await execute(result([item('Provider D')],7),'restart-d');await execute(result([item('Provider D')],7),'restart-d-retry');
    assert.equal((await scalar('select source_data from provider_source_entities where id=$1',[entity.id])).source_data.name,'Provider D');assert.equal(Number((await scalar('select count(*)::int count from provider_source_entities where id=$1',[entity.id])).count),1);assert.equal(Number((await scalar(`select count(*)::int count from provider_source_corrections where source_entity_id=$1 and status='active'`,[entity.id])).count),1);assert.equal(Number((await scalar('select count(*)::int count from provider_source_local_observations where source_entity_id=$1',[entity.id])).count),1);
    assert.equal((await scalar('select cursor from sync_streams where id=$1',[stream])).cursor.page,7);
    const historical='56f00000-0000-4000-8000-000000000004';await pool.query(`insert into sync_streams(id,provider_championship_id,phase,state,cursor_version,cursor,historical_state) values($1,$2,'historical','ready',1,'{}','{"recent_catchup_queue":[],"deep_history_cursor":{}}') on conflict(provider_championship_id,phase) do nothing`,[historical,link]);await pool.query(`insert into provider_acquisition_state(provider_championship_id,bootstrap_state,recent_catchup_state,deep_history_state,deep_history_season) values($1,'complete','complete','complete',1968) on conflict(provider_championship_id) do nothing`,[link]);
    const app=Fastify(),secret='lot-5-6-g-real-postgres-secret-at-least-32-chars';registerAdminAuth(app,secret);await app.register(providerAcquisitionAdminRoutes,{admin:new AcquisitionAdminService(),protection,scheduler});const headers={authorization:`Bearer ${signAdminToken({sub:'api-maintainer',role:'admin',exp:Math.floor(Date.now()/1000)+60},secret)}`};
    const detail=await app.inject({url:`/api/v1/admin/provider-source-entities/${entity.id}`,headers});assert.equal(detail.statusCode,200);assert.equal(Object.hasOwn(detail.json().entity,'source_data'),false);assert.equal(detail.json().manual_correction_active,true);
    const correctionResponse=await app.inject({method:'PUT',url:`/api/v1/admin/provider-source-entities/${entity.id}/corrections/status`,headers,payload:{override_value:'verified',reason:'API verification'}});assert.equal(correctionResponse.statusCode,200);assert.equal(correctionResponse.json().actor_id,'api-maintainer');assert.equal((await scalar('select source_data from provider_source_entities where id=$1',[entity.id])).source_data.status,'scheduled');
    const observationResponse=await app.inject({method:'PUT',url:`/api/v1/admin/provider-source-entities/${entity.id}/observations/api:review`,headers,payload:{kind:'review_note',details:{verified:true},reason:'API verification'}});assert.equal(observationResponse.statusCode,200);assert.equal(observationResponse.json().actor_id,'api-maintainer');
    assert.equal((await app.inject({method:'POST',url:`/api/v1/admin/provider-source-entities/${entity.id}/resync`,headers})).statusCode,202);assert.equal((await scalar("select historical_state->'recent_catchup_queue'->0->>'year' season from sync_streams where id=$1",[historical])).season,'1969');
    assert.equal((await app.inject({method:'POST',url:`/api/v1/admin/provider-championships/${link}/acquisition/rebuild-history`,headers,payload:{from_season:1965,confirmed:true}})).statusCode,202);assert.equal(Number((await scalar('select deep_history_season from provider_acquisition_state where provider_championship_id=$1',[link])).deep_history_season),1965);
    assert.equal((await app.inject({method:'POST',url:`/api/v1/admin/provider-championships/${link}/acquisition/resume-history`,headers})).statusCode,202);await app.close();
    console.log('Phase B : nouveau processus, reprise, provenance, cardinalités et retry idempotent : OK');
  }
}finally{await pool.end();}
